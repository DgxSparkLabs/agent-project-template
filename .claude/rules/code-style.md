---
description: Code style rules
paths: "**/*.{ts,js,py,go,rs,java,c,cpp,h}"
---

# Code Style

Fill in language-specific rules below. Examples to consider — keep only what this project actually enforces:

- **Indentation** — spaces vs tabs, width
- **Naming** — camelCase, snake_case, PascalCase by entity type
- **Imports** — order, grouping, absolute vs relative
- **File / function size** — soft limits before splitting

If a rule isn't enforced, referenced, or used in review, delete it. Path-scoped rules only load when matching files are open, so keep this file focused on the languages the `paths:` glob covers.
